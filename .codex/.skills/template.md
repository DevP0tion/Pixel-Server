# Skill Name
스킬 이름

## Purpose
무엇을 해결하는 스킬인지, 언제 쓰는지.

## Inputs
- `param1` (type): 역할/유효 값
- `param2` (type, optional): 역할/기본값

## Behavior
- 처리 로직 개요 (단계별 요약)
- 외부 의존성 (API, 파일, DB 등)

## Commands / API
- 사용 명령어 또는 엔드포인트와 예시
- 포맷: `command --flag value` 또는 `POST /path { body }`

## Output
- 반환 형식/구조 예시
- 에러 케이스 및 메시지 규칙

## Examples
```bash
# 성공 사례
command --param1 foo --param2 bar

# 실패/에러 사례
command --param1 bad
